# 🐪 Automating Invoice Processing with Apache Camel + Gemini AI 🐫
The solution picks up PDF/image invoices from SFTP, converts them to Base64, and sends them to Gemini for OCR and data extraction. Gemini returns clean, structured JSON that can be consumed by downstream systems with almost no manual effort.
